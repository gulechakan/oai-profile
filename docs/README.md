# Profile Notes

- This repository is based on Ubuntu22.
- It reserves one d430 compute node. 
- `deploy-oai-cn5g.sh` script is used to deploy the OAI 5G Core Network. It sets up the node for OAI deployment. It creates a directory `mydata` to deploy them and then clones a modified latest version (currently v2.2.0) of the core network.